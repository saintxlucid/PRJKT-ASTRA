# Legacy Capability Plugin Configuration
# Auto-generated template for ASTRA legacy integration
# Sacred Code 333: All side-effects recorded in audit log

name: "tier0"
version: "legacy-compat-1.0"
description: "Legacy capability wrapper for tier0 module"

capabilities:
  - name: "tier0.info"
    description: "Query tier0 capability information (read-only)"
    side_effects: false
    consent_required: false
    audit: true
    
  - name: "tier0.run"
    description: "Execute tier0 capability with potential side effects"
    side_effects: true
    consent_required: true
    audit: true

observability:
  emit_events: true
  metrics_enabled: true
  log_level: "info"

security:
  sacred_code: "333"
  hard_gate: true
  consent_gated: true
  audit_trail_enabled: true

metadata:
  integration_type: "legacy-adapter"
  requires_warmup: false
  timeout_ms: 30000

