apiVersion: v1
kind: Secret
metadata: { name: astra-secrets }
type: Opaque
stringData:
  OPENAI_KEY: "..."   # if used
  # add model endpoints, signing keys, etc.
---
apiVersion: v1
kind: ConfigMap
metadata: { name: astra-config }
data:
  ASTRA_ENV: "production"
  TOKEN_BUDGET: "3000"
  OUTPUT_RESERVE: "512"